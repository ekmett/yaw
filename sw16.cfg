FEATURES {
    STARTADDRESS: default = $9d89;
}
MEMORY {
    RAM: file = %O, start = %S, size = $9f00-%S;
}
SEGMENTS {
    ZEROPAGE: load = ZP, type = zp;
    DATA: load = MAIN, type = rw, start = $9d89;
    CODE: load = MAIN, type = ro, start = $9d85;
}
